#include "main.h"

/**
 * _isupper - check if a letter is upper
 * @x: The number to be checked
 *
 * Return: 1 for the uppercase and 0 for otherwise
 */
int _isupper(int x)
{
	if (x >= 'A' && x <= 'Z')
	{
	return (1);
	}
	return (0);
}
