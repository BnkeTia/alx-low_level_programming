#include "main.h"
#include <stdio.h>

/**
 * main - A function prints a name
 * @argc: argument counter
 * @argv: argument vector
 * Return: 0 (complete)
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
